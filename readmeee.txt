++ описание системы. 
постановка задачи
двузвенный манипулятор на неподвижном основании, 120 160
управление - два угла тета
ограничения плюс минус 0.1 рад изменение за шаг. 

цель: достичь точки радиусом 15.

1. Первая попытка через нейросеть сделали первый результат, просто пытались обучить модель в дискретном action space из фиксированного положения в фикс цель. 

Мы убедились, что задача решаема в данной постановке и принялись писать собственное решение.

2. Вторая итерация.
Мы написали собственную программу, которая выводит метрики и реализует обучение через Reinforce. 
Мы принялись учить манипулятор достигать фиксированной цели из фииксированного положения с дискретным action space. Дискретизация поля действий - 7 штук. 128 нейронов в два слоя. Модель принимает sin, cos, dx, dy. (обьяснить, почему такие входы) Модель научилась достигать цели со 100% вероятностью за 16 эпизодов. Тесты подвердили стабильность системы. LR = 0.001, gamma = 0.95.

++ reinforce
++ gradient

собираемые метрики: 
sigma, total reward for episode, success rate, steps/episode.
average distance for episode, 

ревард:
distance_penalty=0.01,
goal_reward=10.0,
episode_failure_penalty=5.0,
action_l2_scale=0.0,
action_delta_scale=0.0,








3. Мы принялись учить манипулятор достигать цели из случайного положения с непрерывным гаусовским action space.
полиси - гаусоское двумерное распределение. sin cos, x, y target, dx, dy. предсказывает 4 параметра - 2 mean, 2 sigma. у сетки две головы: одна два мин, вторая два стд. изза ограничения физического на углы 0.1 рад мы это делаем с помощью ограничения мин через танх. и теперь оно от -0.1 до 0.1. но изза того что ограничение только на мин, то может случиться такое, что насеплится действие за рамками ограничений. и тогда клип.

изначально гаус распредл = 0, 1 sigma. 

gamma 0.99, LR 0.001

нейросеть нам предложила (returns - np.mean(reurens)) / std(returns), но мы поняли что вычитание в текущем эпизоде np.mean(reurens) не является бейзлайном и при делении на std(returns) мы по сути меняем lr на каждом шагу, что может привести к нестабильности. Пожтому мы решили использовать буффер returns, и уже (returns - np.mean(buffer)) где buffer - значения с прошлых эпизодов 